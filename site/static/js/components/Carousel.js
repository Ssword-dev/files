// i still dont have a vdom. this is basically just
// raw dom stuff still.
import h from "../modules/dom-utils";

function createCarouselState() {
  const nullElement = null;
  const state = h.reactive();
}

function CarouselCurrentElement({ carouselState }) {}
